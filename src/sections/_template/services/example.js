/**
 * Exemple de service — logique métier de la section.
 * Imports core disponibles :
 *   - require('../../../core/utils/logger')
 *   - require('../../../core/utils/normalize')
 *   - require('../../../core/repositories/history')
 *   - require('../../../core/repositories/users')
 *   - require('../../../config/env')
 */

function exampleService() {
  return { ok: true };
}

module.exports = { exampleService };
